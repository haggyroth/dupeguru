__version__ = "4.19.0"
__appname__ = "dupeGuru"
# QStandardPaths derives the application data directory from these two names, so every
# front end must use the same pair or each gets its own cache directory. Defined here
# rather than in run.py because the CLI needs them too and does not import it.
__orgname__ = "Hardcoded Software"
